rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 임시 규칙: Excel 데이터 업로드를 위해 모든 접근 허용
    // ⚠️ 경고: 이 규칙은 보안이 취약합니다!
    // ⚠️ Excel 업로드 완료 후 즉시 firestore.rules.auth로 변경하세요!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
